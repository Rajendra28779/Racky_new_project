import { SearchTextHighlighterPipe } from './search-text-highlighter.pipe';

describe('SearchTextHighlighterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTextHighlighterPipe();
    expect(pipe).toBeTruthy();
  });
});
